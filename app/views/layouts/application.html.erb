<!DOCTYPE html>
<html>
<head>
  <title>Travel</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav>
    <%= link_to "Index", root_path(params.except(:action, :controller, :background)) %>
    <%= link_to "About", about_path(params.except(:action, :controller, :background)) %>
    <%= link_to "Contact", contact_path(params.except(:action, :controller, :background)) %>
  </nav>

<%= yield %>

</body>
</html>
